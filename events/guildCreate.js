
const {
    MessageEmbed, Message,
} = require('discord.js');

module.exports = {
	name: 'guildCreate',
	execute(guild) {
		console.log(`Added to ${guild.name}`);

		const emb = new MessageEmbed
			
	},
};