const {
    SlashCommandBuilder
} = require('@discordjs/builders');

const {
    MessageEmbed,
} = require('discord.js');

module.exports = {
    async getSongs(client, interaction, cache) {
        // get songs somehow
    }
    
}